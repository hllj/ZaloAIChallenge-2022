work_dir: ${hydra:runtime.cwd}

name: baseline-swin_large_patch4_window12_384
name_time: 2022-11-25-11-10-23
checkpoint: ./outputs3/last.ckpt
crop_size: 384

images_dir: data/public_test_2/images/*

hydra:
  run:
    dir: ./outputs3/inference_${name}/${name_time}
